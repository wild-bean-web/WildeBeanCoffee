Place 20 PNG files named exactly:
  stamp1.png, stamp2.png, ... stamp20.png

Earned stamps load from /images/RewardIcons/Stamps/stamp{N}.png

Optional placeholder for empty slots (otherwise a built-in bean SVG is used):
  placeholder-beans.png

Also add:
  ../ApplyReward/Apply-reward.png
  ../EmptyState/empty-state.png
And at project root:
  /animations/Confetti.json
