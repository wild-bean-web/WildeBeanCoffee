/** Bean Stamps — online loyalty (must match client copy in terms). */
export const LOYALTY_STAMPS_PER_REWARD = 20;
/** Minimum order total after tax (USD) to earn one stamp. */
export const LOYALTY_QUALIFY_MIN_TOTAL = 10;
/** Max pre-tax discount on the single rewarded line (USD). */
export const LOYALTY_FREE_ITEM_MAX_PRE_TAX = 15;
